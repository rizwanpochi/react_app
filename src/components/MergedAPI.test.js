import { render, screen } from '@testing-library/react';
import MergedAPI from './MergedAPI';

describe('api test 3', () => {
    it("test", async () => {
        render(<MergedAPI />);
    }); 
  });
