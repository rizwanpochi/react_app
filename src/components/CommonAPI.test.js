import { render, screen } from '@testing-library/react';
import CommonAPI from './CommonAPI';

describe('api test 2', () => {
    it("test", async () => {
        render(<CommonAPI />);
    }); 
  });
